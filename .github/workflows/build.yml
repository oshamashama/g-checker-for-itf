name: Test
on:
  push:
    branches:
      - main

jobs:
  react-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Dependencies
        run: |
          cd react-visualizer
          ls
          npm ci
      - name: Build
        run: |
          npm run deploy
          git push origin main
